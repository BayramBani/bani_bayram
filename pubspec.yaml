name: bani_bayram
description: A new test Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Firebase
  firebase_core: ^1.18.0
  firebase_auth: ^3.3.20
  flutterfire_ui: ^0.4.2
  cloud_firestore: ^3.1.18
  firebase_storage: ^10.2.18
  # Firebase Social Login
  google_sign_in: ^5.3.3
  google_sign_in_web: ^0.10.1+3
  # State Management
  provider: ^6.0.3
  shared_preferences: ^2.0.15
  stacked: ^2.3.12
 # Tools
  json_annotation: ^4.5.0
  google_fonts: ^3.0.1
  material_color_generator: ^1.1.0
  flutter_svg: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  logger: ^1.1.0 # for debug
  json_serializable: ^6.2.0
  build_runner: ^2.1.11

flutter:
  uses-material-design: true
  assets:
    - assets/icons/