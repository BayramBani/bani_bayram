name: bani_bayram
description: A new test Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^1.18.0
  flutterfire_ui: ^0.4.2
  firebase_auth: ^3.3.20
  google_fonts: ^3.0.1
  material_color_generator: ^1.1.0
  flutter_svg: ^1.1.0
  provider: ^6.0.3
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  logger: ^1.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/